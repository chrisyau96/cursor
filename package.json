{
  "name": "momentum",
  "private": true,
  "version": "56.0.0",
  "description": "Momentum habit tracker — PWA and Capacitor store shell",
  "scripts": {
    "test:launch": "node scripts/test-launch.mjs",
    "uat": "node scripts/uat-buttons.mjs",
    "build:www": "node scripts/sync-www.mjs",
    "cap:sync": "npm run build:www && npx cap sync",
    "native:apply": "node scripts/apply-native.mjs"
  }
}
